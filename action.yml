name: "Jaiye Agent Check"
description: "Check for file ownership conflicts between AI coding agents"
inputs:
  base_branch:
    description: "Base branch to compare against"
    required: false
    default: "main"
runs:
  using: "composite"
  steps:
    - run: npx jaiye-agent check --base ${{ inputs.base_branch }}
      shell: bash
